# jest-puppeteer-demo :fire:

- using [power-assert](https://www.npmjs.com/package/power-assert)
- using [jest-puppeteer](https://github.com/smooth-code/jest-puppeteer)
- running koa server with `child_process` 